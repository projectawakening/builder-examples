procs:
  contracts:
    cwd: packages/contracts
    shell: pnpm run dev
  client:
    cwd: packages/client
    shell: pnpm run dev
  mock-data:
    cwd: packages/mock-data
    shell: pnpm run dev
  explorer:
    cwd: packages/contracts
    shell: pnpm explorer
